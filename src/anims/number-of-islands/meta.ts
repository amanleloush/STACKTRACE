import type { AnimMeta } from '~/lib/anim/types';

const meta: AnimMeta = {
  id: 'number-of-islands',
  title: 'Number of Islands — flood fill DFS',
  caption: 'Scan the grid. On each unvisited ',
  tags: ['graph'],
  pillar: 'dsa',
  phase: '05-dfs',
  order: 2,
  premium: false,
};

export default meta;
