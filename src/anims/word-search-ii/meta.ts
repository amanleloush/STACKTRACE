import type { AnimMeta } from '~/lib/anim/types';

const meta: AnimMeta = {
  id: 'word-search-ii',
  title: 'Word Search II — grid DFS pruned by trie',
  caption: 'Trie of {oath, oat, eat}. DFS the grid; each step requires the cell',
  tags: ['tree', 'string'],
  pillar: 'dsa',
  phase: '11-trie',
  order: 4,
  premium: false,
};

export default meta;
