import type { AnimMeta } from '~/lib/anim/types';

const meta: AnimMeta = {
  id: 'cap',
  title: 'CAP — partition simulator',
  caption:
    'Three replicas. Drop the link between them, then choose CP (refuse) or AP (serve stale).',
  tags: ['distsys'],
  pillar: 'systems',
  phase: '05-distributed-systems',
  order: 22,
  premium: false,
};

export default meta;
