import type { AnimMeta } from '~/lib/anim/types';

const meta: AnimMeta = {
  id: 'sharding',
  title: 'Sharding — range / hash / geo',
  caption:
    'Same 24 keys, three strategies. Watch the hot user concentrate load in range sharding but spread in hash.',
  tags: ['sharding', 'db', 'distsys'],
  pillar: 'systems',
  phase: '06-hld-patterns',
  order: 31,
  premium: false,
};

export default meta;
