import type { AnimMeta } from '~/lib/anim/types';

const meta: AnimMeta = {
  id: 'word-dictionary',
  title: 'Word Dictionary — wildcard search',
  caption: 'Search ',
  tags: ['tree', 'string'],
  pillar: 'dsa',
  phase: '11-trie',
  order: 3,
  premium: false,
};

export default meta;
