import type { AnimMeta } from '~/lib/anim/types';

const meta: AnimMeta = {
  id: 'merge-k-sorted',
  title: 'Merge K sorted lists — min-heap of heads',
  caption: 'Put each list',
  tags: ['sorting'],
  pillar: 'dsa',
  phase: '07-heap',
  order: 2,
  premium: false,
};

export default meta;
