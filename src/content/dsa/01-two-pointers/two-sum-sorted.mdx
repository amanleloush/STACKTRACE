---
title: "Two Sum on sorted array"
phase: "01-two-pointers"
order: 2
tags: []
premium: false
previewParagraphs: 4
estimatedMinutes: 5
prerequisites: []
summary: "On a sorted array, two pointers from the ends find a pair summing to a target in O(n) time and O(1) space — each move discards a row or column of provably-wrong pairs."
complexity:
  time: "O(n)"
  space: "O(1)"
commonPitfalls:
  - "Forgetting the array is already sorted — if unsorted, use a hash-map in O(n) time / O(n) space."
  - "LeetCode 167 returns 1-indexed values — read carefully."
  - "With duplicates and 'all unique pairs', advance both pointers past equal neighbours after each match."
  - "For 3Sum / 4Sum, the outer loop fixes one (or two) elements; only the inner pair is two-pointer."
relatedAnims: []
---

## Problem
Given a **sorted** array and a target `T`, return the indices of two elements that sum to `T` (or report that none exist). O(1) extra space.

## Intuition
If `arr[l] + arr[r] < T`, the smallest available pair on the right (`arr[l] + arr[r]`) already underflows — every pair using `l` underflows too, so `l` must move right. Symmetrically, if the sum overflows, `r` must move left. Each move discards a whole row or column of candidate pairs, so the algorithm visits at most `n` pointer positions total. The invariant: every pair we've skipped is provably not the answer.

## Algorithm
1. Start `l = 0`, `r = n - 1`.
2. Compute `s = arr[l] + arr[r]`.
3. If `s == T`, return `(l, r)`.
4. If `s < T`, increment `l`. Else decrement `r`.
5. Repeat until `l >= r`.

```python
def two_sum_sorted(arr, T):
    l, r = 0, len(arr) - 1
    while l < r:
        s = arr[l] + arr[r]
        if s == T:
            return (l, r)
        if s < T:
            l += 1
        else:
            r -= 1
    return None
```

## Walkthrough
On `[1, 3, 4, 6, 8, 11]`, target `10`:

- Step 1 — `l=0, r=5`: `1 + 11 = 12 > 10` → `r--`.
- Step 2 — `l=0, r=4`: `1 + 8 = 9 < 10` → `l++`.
- Step 3 — `l=1, r=4`: `3 + 8 = 11 > 10` → `r--`.
- Step 4 — `l=1, r=3`: `3 + 6 = 9 < 10` → `l++`.
- Step 5 — `l=2, r=3`: `4 + 6 = 10` → return `(2, 3)`.

<Anim id="two-sum-sorted" />

## Complexity

- time **O(n)**
- space **O(1)**

## Pitfalls
- Forgetting the array is **already sorted** — if it isn't, a hash-map solution in O(n) time / O(n) space is the right call.
- LeetCode 167 uses **1-indexed** return values — read the problem statement carefully.
- With duplicates and a "find all unique pairs" twist, advance `l` and `r` past equal neighbours after each match.
- For 3Sum / 4Sum, the outer loop fixes one (or two) elements; only the inner pair is two-pointer. Don't try to two-pointer everything at once.

## Practice — LeetCode

- [167. Two Sum II — Input Array Is Sorted (Easy)](https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/) — the canonical statement; mind the 1-indexing.
- [1. Two Sum (Easy)](https://leetcode.com/problems/two-sum/) — unsorted variant; hash-map is the cleaner path.
- [977. Squares of a Sorted Array (Easy)](https://leetcode.com/problems/squares-of-a-sorted-array/) — merge from both ends because squares grow at both extremes.
- [633. Sum of Square Numbers (Medium)](https://leetcode.com/problems/sum-of-square-numbers/) — same template over the implicit range `[0, sqrt(c)]`.
- [653. Two Sum IV — Input is a BST (Easy)](https://leetcode.com/problems/two-sum-iv-input-is-a-bst/) — in-order iterators from both ends mimic two pointers.
- [88. Merge Sorted Array (Easy)](https://leetcode.com/problems/merge-sorted-array/) — write from the tail to avoid overwriting.
- [532. K-diff Pairs in an Array (Medium)](https://leetcode.com/problems/k-diff-pairs-in-an-array/) — same idea with difference instead of sum.
- [15. 3Sum (Medium)](https://leetcode.com/problems/3sum/) — fix one element, two-pointer the rest.
- [16. 3Sum Closest (Medium)](https://leetcode.com/problems/3sum-closest/) — track the running best difference.
- [18. 4Sum (Medium)](https://leetcode.com/problems/4sum/) — two nested fixed indices, then two-pointer.
