---
title: "Reverse array in place"
phase: "01-two-pointers"
order: 1
tags: []
premium: false
previewParagraphs: 4
estimatedMinutes: 5
prerequisites: []
summary: "Reverse an array in place with O(1) extra space using two pointers that swap from both ends and meet in the middle."
complexity:
  time: "O(n)"
  space: "O(1)"
commonPitfalls:
  - "Using `l <= r` swaps the middle with itself — harmless but wasteful, and a bug if the swap has side effects."
  - "Off-by-one when seeding `r = n` instead of `r = n - 1`."
  - "Immutable strings (Java, Python str) can't be swapped in place — convert to a char array first."
  - "Reversing a subrange `[i, j]` is the same loop with different seeds — parameterize, don't rewrite."
relatedAnims: []
---

## Problem
Given an array, reverse it in place using O(1) extra space.

## Intuition
The element at index `i` ultimately belongs at index `n - 1 - i`. Pair them up: swap `(0, n-1)`, then `(1, n-2)`, and so on, until the two indices meet in the middle. Each swap fixes two positions, so the loop runs exactly `n/2` times. The invariant is that everything outside `[l, r]` is already reversed; everything inside is untouched original order.

## Algorithm
1. Start `l = 0`, `r = n - 1`.
2. While `l < r`: swap `arr[l]` and `arr[r]`, then `l++`, `r--`.
3. Stop when the pointers meet or cross.

```python
def reverse(arr):
    l, r = 0, len(arr) - 1
    while l < r:
        arr[l], arr[r] = arr[r], arr[l]
        l += 1
        r -= 1
    return arr
```

## Walkthrough
On `[1, 2, 3, 4, 5]`:

- Step 1 — `l=0, r=4`: swap `1` and `5` → `[5, 2, 3, 4, 1]`.
- Step 2 — `l=1, r=3`: swap `2` and `4` → `[5, 4, 3, 2, 1]`.
- Step 3 — `l=2, r=2`: pointers meet, loop exits; middle element stays in place.

For even-length arrays `[1, 2, 3, 4]`, the loop ends with `l=2, r=1` after two swaps.

<Anim id="reverse-array" />

## Complexity

- time **O(n)**
- space **O(1)**

## Pitfalls
- Using `l <= r` swaps the middle element with itself — harmless but wasteful, and a bug if the swap has side effects.
- Off-by-one when seeding `r = n` instead of `r = n - 1`.
- For strings in immutable-string languages (Java, Python `str`), you can't swap in place — convert to a char array first.
- Reversing only a subrange `[i, j]` is the same loop with different seeds — don't rewrite the logic, parameterize it.

## Practice — LeetCode

- [344. Reverse String (Easy)](https://leetcode.com/problems/reverse-string/) — the canonical in-place reverse on a char array.
- [541. Reverse String II (Easy)](https://leetcode.com/problems/reverse-string-ii/) — reverse every K-th chunk; reuse the subrange variant.
- [345. Reverse Vowels of a String (Easy)](https://leetcode.com/problems/reverse-vowels-of-a-string/) — two pointers, skip non-vowels on each side.
- [917. Reverse Only Letters (Easy)](https://leetcode.com/problems/reverse-only-letters/) — same trick, letter-only filter.
- [125. Valid Palindrome (Easy)](https://leetcode.com/problems/valid-palindrome/) — compare-then-advance instead of swap.
- [557. Reverse Words in a String III (Easy)](https://leetcode.com/problems/reverse-words-in-a-string-iii/) — reverse each word's range independently.
- [189. Rotate Array (Medium)](https://leetcode.com/problems/rotate-array/) — three reverses: whole, prefix, suffix.
- [151. Reverse Words in a String (Medium)](https://leetcode.com/problems/reverse-words-in-a-string/) — reverse the whole string, then each word.
- [234. Palindrome Linked List (Easy)](https://leetcode.com/problems/palindrome-linked-list/) — reverse the second half, then compare halves.
- [25. Reverse Nodes in K-Group (Hard)](https://leetcode.com/problems/reverse-nodes-in-k-group/) — pointer surgery on linked-list chunks.
