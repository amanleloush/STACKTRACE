---
title: "Topological Sort (Kahn)"
phase: "08-graphs"
order: 1
tags: ["graph"]
premium: false
previewParagraphs: 4
estimatedMinutes: 8
prerequisites: []
summary: "Kahn's algorithm peels zero-in-degree nodes off a DAG in linear time and detects cycles for free if the output length is less than V."
complexity:
  time: "O(V + E)"
  space: "O(V + E)"
commonPitfalls:
  - "Cycle detection comes for free — if len(out) < n, you have a cycle. Don't add a separate DFS pass."
  - "Multiple valid orderings exist — use a min-heap instead of a queue if the problem demands the lexicographically smallest."
  - "DFS-based topo (post-order + reverse) is equally valid and sometimes cleaner; know both."
  - "Self-loops count as a cycle (in-degree 1 from the get-go and never reachable from zero)."
  - "Disconnected DAG — works fine; every connected component contributes its zero-in-degree nodes."
relatedAnims: []
---

## Problem
Given a directed acyclic graph (DAG), output a linear ordering of nodes such that every edge `u → v` has `u` before `v`. If the graph has a cycle, report it.

## Intuition
A node is "ready" once all of its prerequisites have been emitted — i.e., when its **in-degree drops to zero**. Kahn's algorithm peels off zero-in-degree nodes one at a time, like leaves off a tree, decrementing in-degrees as it goes. The invariant: **at every step, the queue contains exactly the nodes whose prerequisites are all already emitted.** If the loop ends without visiting every node, the leftover nodes form a cycle.

## Algorithm
Build the in-degree map. Seed a queue with every zero-in-degree node. Pop, append to output, and decrement each neighbor's in-degree; when a neighbor hits zero, enqueue it. If the output length equals V, return it; otherwise the graph has a cycle.

```python
from collections import deque, defaultdict

def topo_sort(n, edges):
    g = defaultdict(list)
    indeg = [0] * n
    for u, v in edges:
        g[u].append(v)
        indeg[v] += 1
    q = deque(i for i in range(n) if indeg[i] == 0)
    out = []
    while q:
        u = q.popleft()
        out.append(u)
        for v in g[u]:
            indeg[v] -= 1
            if indeg[v] == 0:
                q.append(v)
    return out if len(out) == n else []   # [] signals a cycle
```

## Walkthrough
Example: `n = 6`, edges `[(5,2), (5,0), (4,0), (4,1), (2,3), (3,1)]`.

1. In-degrees: 0→1, 1→2, 2→1, 3→1, 4→0, 5→0. Queue starts with [4, 5].
2. Pop 4 → out=[4]. Decrement 0 (→0) and 1 (→1). Queue=[5, 0].
3. Pop 5 → out=[4, 5]. Decrement 2 (→0) and 0 (already 0). Queue=[0, 2].
4. Pop 0 → out=[4, 5, 0]. No outgoing edges. Pop 2 → out=[4, 5, 0, 2]. Decrement 3 (→0). Queue=[3].
5. Pop 3 → decrement 1 (→0). Pop 1 → out=[4, 5, 0, 2, 3, 1]. Length = 6 → done.

<Anim id="topo-sort" />

## Complexity

time **O(V + E)** · space **O(V + E)**

## Pitfalls
- **Cycle detection comes for free** — if `len(out) < n`, you have a cycle. Don't add a separate DFS pass.
- **Multiple valid orderings exist** — use a min-heap instead of a queue if the problem demands the lexicographically smallest.
- **DFS-based topo** (post-order + reverse) is equally valid and sometimes cleaner; know both.
- **Self-loops** count as a cycle (in-degree 1 from the get-go and never reachable from zero).
- **Disconnected DAG** — works fine; every connected component contributes its zero-in-degree nodes.

#### Practice — LeetCode
- [207 Course Schedule (Med)](https://leetcode.com/problems/course-schedule/) — pure cycle detection via topo.
- [210 Course Schedule II (Med)](https://leetcode.com/problems/course-schedule-ii/) — emit the order, not just feasibility.
- [269 Alien Dictionary (Hard)](https://leetcode.com/problems/alien-dictionary/) — derive edges from adjacent word pairs.
- [444 Sequence Reconstruction (Med)](https://leetcode.com/problems/sequence-reconstruction/) — unique topo order check.
- [802 Find Eventual Safe States (Med)](https://leetcode.com/problems/find-eventual-safe-states/) — reverse-graph topo.
- [1136 Parallel Courses (Med)](https://leetcode.com/problems/parallel-courses/) — topo with semester levels.
- [1203 Sort Items by Groups Respecting Dependencies (Hard)](https://leetcode.com/problems/sort-items-by-groups-respecting-dependencies/) — two-level topo.
- [2127 Maximum Employees to Be Invited (Hard)](https://leetcode.com/problems/maximum-employees-to-be-invited-to-a-meeting/) — topo to peel off chains, then handle cycles.
- [310 Minimum Height Trees (Med)](https://leetcode.com/problems/minimum-height-trees/) — peel leaves (degree-1) inward.
- [329 Longest Increasing Path in a Matrix (Hard)](https://leetcode.com/problems/longest-increasing-path-in-a-matrix/) — topo on implicit DAG.
