---
title: "Subsets"
phase: "06-backtracking"
order: 1
tags: []
premium: false
previewParagraphs: 4
estimatedMinutes: 5
prerequisites: []
summary: "Walk the array; at each index choose include-or-skip. The DFS tree has depth n and 2^n leaves — every node is itself a valid subset."
complexity:
  time: "O(n · 2ⁿ)"
  space: "O(n)"
commonPitfalls:
  - "Appending path instead of path[:] — every result ends up pointing at the same list, which is empty at the end."
  - "Using start = i + 0 instead of i + 1 — generates combinations with repetition."
  - "For Subsets II (duplicates allowed in input), forgetting to sort first and add if i > start and nums[i] == nums[i-1]: continue."
  - "Recording only at leaves — you'll miss every proper-prefix subset including []."
  - "Confusing subsets with permutations — subsets care about set membership, not order."
relatedAnims: []
---

## Problem
Given a set of `n` distinct integers, return every possible subset (the power set) — `2ⁿ` subsets in total.

## Intuition
Walk the array left to right. At each index you make one of two decisions: **include** this element in the current subset, or **skip** it. The DFS tree has depth `n` and `2ⁿ` leaves — each leaf is one subset. The **invariant** is that `path` always holds the include-decisions for indices `[0..i)`; on every recursive entry, `path` is a valid subset of the prefix.

## Algorithm
A `start` pointer marks where we are in the array. We record `path` at every node (not just leaves) because every node *is* a valid subset.

```python
def subsets(nums):
    res, path = [], []

    def dfs(start):
        res.append(path[:])          # snapshot — current subset
        for i in range(start, len(nums)):
            path.append(nums[i])     # choose
            dfs(i + 1)               # explore
            path.pop()               # un-choose

    dfs(0)
    return res
```

## Walkthrough
1. `nums = [1, 2, 3]`. Call `dfs(0)`. Record `[]`.
2. Pick `1` → `path = [1]`, recurse with `start=1`. Record `[1]`.
3. From there pick `2` → `[1,2]`, record; pick `3` → `[1,2,3]`, record; pop back.
4. Back at `[1]`, pick `3` → `[1,3]`, record; pop. Pop `1`.
5. Back at root, pick `2` → record `[2]`; descend; eventually we've recorded all 8 subsets.

<Anim id="subsets" />

## Complexity

- time **O(n · 2ⁿ)**
- space **O(n)** recursion (output excluded)

## Pitfalls
- Appending `path` instead of `path[:]` — every result ends up pointing at the same list, which is empty at the end.
- Using `start` = `i + 0` instead of `i + 1` — generates combinations with repetition.
- For `Subsets II` (duplicates allowed in input), forgetting to sort first and add `if i > start and nums[i] == nums[i-1]: continue`.
- Recording only at leaves — you'll miss every proper-prefix subset including `[]`.
- Confusing subsets with permutations — subsets care about *set membership*, not order.

## Practice — LeetCode
- [78. Subsets (Medium)](https://leetcode.com/problems/subsets/) — the canonical version.
- [90. Subsets II (Medium)](https://leetcode.com/problems/subsets-ii/) — duplicates: sort + skip.
- [77. Combinations (Medium)](https://leetcode.com/problems/combinations/) — same skeleton, pick exactly k.
- [39. Combination Sum (Medium)](https://leetcode.com/problems/combination-sum/) — reuse allowed, prune by remaining target.
- [40. Combination Sum II (Medium)](https://leetcode.com/problems/combination-sum-ii/) — no reuse + duplicates.
- [216. Combination Sum III (Medium)](https://leetcode.com/problems/combination-sum-iii/) — k numbers summing to n.
- [491. Non-decreasing Subsequences (Medium)](https://leetcode.com/problems/non-decreasing-subsequences/) — dedupe via per-level set.
- [17. Letter Combinations of a Phone Number (Medium)](https://leetcode.com/problems/letter-combinations-of-a-phone-number/) — fixed-depth branching.
- [1980. Find Unique Binary String (Medium)](https://leetcode.com/problems/find-unique-binary-string/) — backtrack on bit choice.
- [320. Generalized Abbreviation (Medium)](https://leetcode.com/problems/generalized-abbreviation/) — abbreviate-or-keep at each char.
