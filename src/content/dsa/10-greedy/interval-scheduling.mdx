---
title: "Interval Merging"
phase: "10-greedy"
order: 5
tags: ["greedy"]
premium: false
previewParagraphs: 4
estimatedMinutes: 6
prerequisites: []
summary: "Sort by start time and sweep — extend the active interval when overlapping, push and reset when disjoint; one running 'end' is all you need."
complexity:
  time: "O(n log n)"
  space: "O(n)"
commonPitfalls:
  - "Sorting by end works for activity selection but breaks merging — you'll miss overlaps that end before a later-starting longer one."
  - "Touching vs overlapping: [1,3] and [3,5] — decide whether to merge based on the problem statement (LC 56 merges them)."
  - "For 'meeting rooms II', don't try to merge — use a min-heap of end times instead."
  - "For inserting a new interval (LC 57), don't re-sort; do a three-phase sweep (before, overlap-and-extend, after)."
  - "Mutating the input list while iterating — make out separate or work on indices."
relatedAnims: []
---

## Problem
Given intervals, merge all overlapping ones into the smallest set of disjoint intervals that covers the same points.

## Intuition
Sort by **start time**. Sweep left to right keeping one "active" interval `[s, e]`. For each new `[s', e']`: if `s' ≤ e`, they overlap — extend `e = max(e, e')`. Otherwise the active interval is done; push it and start a new one with `[s', e']`. The proof: after sorting by start, anything that overlaps the active interval must overlap it on the left, so a single "running end" is all you need. The same scan trivially answers room-count (max active at once), non-overlap count, and intersection enumeration with sibling variants.

## Algorithm
```python
def merge(intervals):
    intervals.sort(key=lambda x: x[0])
    out = []
    for s, e in intervals:
        if out and s <= out[-1][1]:
            out[-1][1] = max(out[-1][1], e)
        else:
            out.append([s, e])
    return out
```

## Walkthrough
Example: `intervals = [[1,3], [2,6], [8,10], [15,18]]` (already sorted by start).

1. **Start.** `out = []`. Take `[1, 3]`: output is empty → **append**. `out = [[1, 3]]`.
2. **`[2, 6]` overlaps.** `2 ≤ out[-1].end = 3` → **merge**: extend last end to `max(3, 6) = 6`. `out = [[1, 6]]`.
3. **`[8, 10]` is disjoint.** `8 > 6` → **append**. `out = [[1, 6], [8, 10]]`.
4. **`[15, 18]` is disjoint.** `15 > 10` → **append**. `out = [[1, 6], [8, 10], [15, 18]]`.
5. **End.** Three merged intervals returned — the input's two overlapping intervals collapsed into `[1, 6]`.

<Anim id="intervals" />

## Complexity

time **O(n log n)** · space **O(n)**

## Pitfalls
- Sorting by end works for activity selection but **breaks** merging — you'll miss overlaps that end before a later-starting longer one.
- Touching vs overlapping: `[1,3]` and `[3,5]` — decide whether to merge based on the problem statement (LC 56 merges them).
- For "meeting rooms II", don't try to merge — use a min-heap of end times instead.
- For inserting a new interval (LC 57), don't re-sort; do a three-phase sweep (before, overlap-and-extend, after).
- Mutating the input list while iterating — make `out` separate or work on indices.

#### Practice — LeetCode
- [56 Merge Intervals (Med)](https://leetcode.com/problems/merge-intervals/) — canonical sweep.
- [57 Insert Interval (Med)](https://leetcode.com/problems/insert-interval/) — three-phase variant.
- [252 Meeting Rooms (Easy)](https://leetcode.com/problems/meeting-rooms/) — sort and check adjacent overlaps.
- [253 Meeting Rooms II (Med)](https://leetcode.com/problems/meeting-rooms-ii/) — heap of end times.
- [435 Non-overlapping Intervals (Med)](https://leetcode.com/problems/non-overlapping-intervals/) — sort by end, count keeps.
- [763 Partition Labels (Med)](https://leetcode.com/problems/partition-labels/) — last-occurrence map → merge.
- [986 Interval List Intersections (Med)](https://leetcode.com/problems/interval-list-intersections/) — two-pointer on two sorted lists.
- [759 Employee Free Time (Hard)](https://leetcode.com/problems/employee-free-time/) — merge, then gaps.
- [1851 Minimum Interval to Include Each Query (Hard)](https://leetcode.com/problems/minimum-interval-to-include-each-query/) — offline sort + heap.
- [2008 Maximum Earnings From Taxi (Med)](https://leetcode.com/problems/maximum-earnings-from-taxi/) — DP on sorted intervals.
