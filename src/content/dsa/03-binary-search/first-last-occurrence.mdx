---
title: "First / Last Occurrence"
phase: "03-binary-search"
order: 2
tags: ["searching"]
premium: false
previewParagraphs: 4
estimatedMinutes: 6
prerequisites: []
summary: "With duplicates, bias the binary search toward the boundary instead of returning on match — search the FFFTTT flip from each side. Two O(log n) passes give first and last indices."
complexity:
  time: "O(log n)"
  space: "O(1)"
commonPitfalls:
  - "Don't return `mid` on match — that's classic search and yields some occurrence, not the boundary."
  - "Validate `arr[lo] == target` after the loop — the index can land just past the run."
  - "Bounds-check `lo < len(arr)` and `hi >= 0` before indexing — the boundary can fall off the ends."
  - "Don't mix the two templates in one function — keep them structurally identical except `>=` vs `<=`."
  - "For 'smallest element > target' (bisect_right), predicate is `arr[mid] > target`, not `>=`."
relatedAnims: []
---

## Problem
Given a sorted array `arr` with possible duplicates and a target value, return the index of the **first** and **last** occurrence of the target. If the target is absent, return `[-1, -1]`.

## Intuition
Classic binary search returns *some* index that matches — but with duplicates we want a specific boundary. The trick is to bias the search: when `arr[mid] == target`, don't stop — keep narrowing toward the boundary. For the leftmost match, push `hi = mid - 1` even on a hit; for the rightmost, push `lo = mid + 1`. The predicate is no longer "found / not found" — it's `FFFTTT` (or `TTTFFF`) and we're hunting the flip.

## Algorithm
1. Run binary search twice — once biased left, once biased right.
2. For the leftmost: when `arr[mid] >= target`, narrow `hi = mid - 1`; else `lo = mid + 1`. After the loop, check `arr[lo] == target`.
3. For the rightmost: when `arr[mid] <= target`, narrow `lo = mid + 1`; else `hi = mid - 1`. After the loop, `hi` is the rightmost match.

```python
def search_range(arr, target):
    def leftmost():
        lo, hi = 0, len(arr) - 1
        while lo <= hi:
            mid = lo + (hi - lo) // 2
            if arr[mid] >= target:
                hi = mid - 1
            else:
                lo = mid + 1
        return lo if lo < len(arr) and arr[lo] == target else -1

    def rightmost():
        lo, hi = 0, len(arr) - 1
        while lo <= hi:
            mid = lo + (hi - lo) // 2
            if arr[mid] <= target:
                lo = mid + 1
            else:
                hi = mid - 1
        return hi if hi >= 0 and arr[hi] == target else -1

    return [leftmost(), rightmost()]
```

## Walkthrough
On `arr = [1, 2, 2, 2, 3, 4, 5, 5, 5, 6]`, `target = 2`:

1. **Phase 1 — leftmost.** `lo=0, hi=9` → `mid=4`, `arr[4]=3 > 2` → push `hi=3`.
2. `lo=0, hi=3` → `mid=1`, `arr[1]=2 == 2` → record `ans=1`, push `hi=0` to keep looking left.
3. `lo=0, hi=0` → `mid=0`, `arr[0]=1 < 2` → `lo=1`. Loop ends. **First = 1.**
4. **Phase 2 — rightmost.** Mirror sweep records `ans` and pushes `lo` right on each match, landing on `mid=3` last. **Last = 3.**
5. Result: `first=1, last=3` — the run of `2`s spans indices `1..3`.

<Anim id="first-last-occurrence" />

## Complexity

- time **O(log n)**
- space **O(1)**

## Pitfalls
- Don't return `mid` as soon as you find the target — that's the classic-search instinct and it gives you *some* occurrence, not the boundary you want.
- After the loop, validate `arr[lo] == target` (or `arr[hi]`) — the index might land just past the run.
- Bounds-check `lo < len(arr)` and `hi >= 0` before indexing — the boundary can fall off the ends.
- Don't mix the two templates in one function — keep the leftmost and rightmost searches structurally identical except for the `>=` / `<=` flip.
- For "smallest element greater than target" (`bisect_right`), the predicate is `arr[mid] > target`, not `>=`.

## Practice — LeetCode

- [34. Find First and Last Position (Medium)](https://leetcode.com/problems/find-first-and-last-position-of-element-in-sorted-array/) — the canonical problem.
- [35. Search Insert Position (Easy)](https://leetcode.com/problems/search-insert-position/) — leftmost insert point.
- [278. First Bad Version (Easy)](https://leetcode.com/problems/first-bad-version/) — leftmost true on a `FFFTTT` predicate.
- [875. Koko Eating Bananas (Medium)](https://leetcode.com/problems/koko-eating-bananas/) — leftmost feasible speed.
- [540. Single Element in a Sorted Array (Medium)](https://leetcode.com/problems/single-element-in-a-sorted-array/) — parity-based predicate.
- [658. Find K Closest Elements (Medium)](https://leetcode.com/problems/find-k-closest-elements/) — binary search on the left endpoint.
- [744. Find Smallest Letter Greater Than Target (Easy)](https://leetcode.com/problems/find-smallest-letter-greater-than-target/) — `bisect_right` on chars.
- [1539. Kth Missing Positive Number (Easy)](https://leetcode.com/problems/kth-missing-positive-number/) — predicate on missing count.
- [1283. Find the Smallest Divisor (Medium)](https://leetcode.com/problems/find-the-smallest-divisor-given-a-threshold/) — leftmost divisor that fits.
- [162. Find Peak Element (Medium)](https://leetcode.com/problems/find-peak-element/) — boundary search on slope direction.
