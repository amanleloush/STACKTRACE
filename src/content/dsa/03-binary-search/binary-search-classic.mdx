---
title: "Classic Binary Search"
phase: "03-binary-search"
order: 1
tags: ["searching"]
premium: false
previewParagraphs: 4
estimatedMinutes: 5
prerequisites: []
summary: "Halve the search range each probe to find a target in a sorted array in O(log n). The invariant is simple: if the target exists, it lives in [lo, hi]."
complexity:
  time: "O(log n)"
  space: "O(1)"
commonPitfalls:
  - "Using `(lo + hi) // 2` risks overflow in fixed-width int languages — prefer `lo + (hi - lo) // 2`."
  - "Mixing up `lo <= hi` with `hi = mid` causes infinite loops. Pick one template and stick to it."
  - "Forgetting to return `-1` when the loop exits — easy to fall off the function."
  - "Assuming sorted input when the problem doesn't say so."
  - "Off-by-one on `hi` — it should be `n - 1` for an inclusive upper bound, not `n`."
relatedAnims: []
---

## Problem
Given a sorted array `arr` and a target value, return the index of the target or `-1` if it's not present.

## Intuition
Because the array is sorted, comparing the target to the middle element tells you which half can be safely discarded. Each step halves the search range, so after at most `⌈log₂ n⌉` probes you've either found the target or proven it isn't there. The invariant is simple: if `target` exists, it lives in `[lo, hi]`.

## Algorithm
1. Initialize `lo = 0`, `hi = n - 1`.
2. While `lo <= hi`:
   - Compute `mid = lo + (hi - lo) // 2`.
   - If `arr[mid] == target`, return `mid`.
   - If `arr[mid] < target`, move `lo = mid + 1`.
   - Else move `hi = mid - 1`.
3. If the loop exits, return `-1`.

```python
def binary_search(arr, target):
    lo, hi = 0, len(arr) - 1
    while lo <= hi:
        mid = lo + (hi - lo) // 2
        if arr[mid] == target:
            return mid
        if arr[mid] < target:
            lo = mid + 1
        else:
            hi = mid - 1
    return -1
```

## Walkthrough
On `arr = [-1, 0, 3, 5, 9, 12]`, `target = 9`:

1. `lo=0, hi=5` → `mid=2`, `arr[2]=3 < 9` → discard left half, `lo=3`.
2. `lo=3, hi=5` → `mid=4`, `arr[4]=9 == 9` → return `4`.

Two probes against six elements — exactly `⌈log₂ 6⌉ = 3` upper bound.

<Anim id="binary-search" />

## Complexity

- time **O(log n)**
- space **O(1)**

## Pitfalls
- Using `(lo + hi) // 2` in languages with fixed-width ints risks overflow — prefer `lo + (hi - lo) // 2`.
- Mixing up `lo <= hi` with `hi = mid` causes infinite loops. Stick to one template.
- Forgetting to return `-1` when the loop exits — easy to fall off the function.
- Assuming the array is sorted when the problem only says "given an array". Re-read the constraints.
- Off-by-one on `hi`: it should be `n - 1` for an inclusive upper bound, not `n`.

## Practice — LeetCode

- [704. Binary Search (Easy)](https://leetcode.com/problems/binary-search/) — the literal template.
- [35. Search Insert Position (Easy)](https://leetcode.com/problems/search-insert-position/) — return `lo` instead of `-1`.
- [374. Guess Number Higher or Lower (Easy)](https://leetcode.com/problems/guess-number-higher-or-lower/) — predicate is the API itself.
- [367. Valid Perfect Square (Easy)](https://leetcode.com/problems/valid-perfect-square/) — search over `[1, n]` for `mid*mid == n`.
- [69. Sqrt(x) (Easy)](https://leetcode.com/problems/sqrtx/) — same trick, return floor.
- [278. First Bad Version (Easy)](https://leetcode.com/problems/first-bad-version/) — leftmost true on a `FFFTTT` predicate.
- [875. Koko Eating Bananas (Medium)](https://leetcode.com/problems/koko-eating-bananas/) — search on the eating speed.
- [1011. Capacity To Ship Packages (Medium)](https://leetcode.com/problems/capacity-to-ship-packages-within-d-days/) — search on capacity.
- [410. Split Array Largest Sum (Hard)](https://leetcode.com/problems/split-array-largest-sum/) — search on the largest subarray sum.
- [2226. Maximum Candies Allocated (Medium)](https://leetcode.com/problems/maximum-candies-allocated-to-k-children/) — search on candies per child.
