---
title: "Search 2D Matrix (Staircase Walk)"
phase: "03-binary-search"
order: 5
tags: ["searching"]
premium: false
previewParagraphs: 4
estimatedMinutes: 5
prerequisites: []
summary: "From the top-right corner, each comparison eliminates a whole row or column. The staircase walk visits at most m + n cells."
complexity:
  time: "O(m + n)"
  space: "O(1)"
commonPitfalls:
  - "Starting at `(0, 0)` or `(m-1, n-1)` doesn't work — both directions increase/decrease together."
  - "Bottom-left `(m-1, 0)` is equally valid with swapped directions — pick one and stick."
  - "For LC 74 (strictly sorted across rows), staircase is correct but suboptimal — use flattened binary search."
  - "Don't confuse with 'kth smallest in sorted matrix' (LC 378) — that's binary search on answer."
  - "Bounds-check `r < m and c >= 0`, not `r <= m`."
relatedAnims: []
---

## Problem
Given an `m × n` matrix where each row is sorted left-to-right and each column is sorted top-to-bottom, return whether a target exists in the matrix.

## Intuition
Start at a corner where one direction increases and the other decreases — the **top-right** (`(0, n-1)`) is the canonical choice. From there: if the current cell is **bigger** than target, you can drop the entire column (move left); if it's **smaller**, you can drop the entire row (move down). Each step eliminates an entire row or column, so the walk visits at most `m + n` cells. It looks like a staircase descending from the top-right corner.

## Algorithm
1. Start at `(r, c) = (0, n - 1)`.
2. While `r < m` and `c >= 0`:
   - If `matrix[r][c] == target`, return true.
   - If `matrix[r][c] > target`, move left (`c -= 1`).
   - Else move down (`r += 1`).
3. Return false.

```python
def search_matrix(matrix, target):
    if not matrix or not matrix[0]:
        return False
    m, n = len(matrix), len(matrix[0])
    r, c = 0, n - 1
    while r < m and c >= 0:
        if matrix[r][c] == target:
            return True
        if matrix[r][c] > target:
            c -= 1
        else:
            r += 1
    return False
```

## Walkthrough
On the 4×4 matrix with `target = 5`:

```
[ 1,  4,  7, 11]
[ 2,  5,  8, 12]
[ 3,  6,  9, 16]
[10, 13, 14, 17]
```

1. **Start at top-right** `(0, 3) = 11`. `11 > 5` → column 3 eliminated, step left to `(0, 2)`.
2. `(0, 2) = 7 > 5` → column 2 eliminated, step left to `(0, 1)`.
3. `(0, 1) = 4 < 5` → row 0 eliminated, step down to `(1, 1)`.
4. `(1, 1) = 5 == target` — return `true`. Four cell visits on a 16-cell matrix.

<Anim id="2d-matrix" />

## Complexity

- time **O(m + n)**
- space **O(1)**

For the stricter LC 74 variant (every row starts after the previous row's end), a true O(log(m·n)) binary search is possible by treating the matrix as a flat sorted array.

## Pitfalls
- **Starting at `(0, 0)` or `(m-1, n-1)`** doesn't work — both directions either increase or decrease, so you can't eliminate.
- The bottom-left corner `(m-1, 0)` is equally valid (swap directions) — but pick one and stick with it.
- For LC 74 (strictly increasing across row boundaries), staircase is correct but suboptimal — use flattened binary search for O(log(m·n)).
- Don't confuse this with "kth smallest in sorted matrix" (LC 378) — that's binary search on the answer with `count_le(mid)` as the predicate.
- Bounds-check inside the loop — `r < m and c >= 0`, not `r <= m`.

## Practice — LeetCode

- [74. Search a 2D Matrix (Medium)](https://leetcode.com/problems/search-a-2d-matrix/) — flattened binary search variant.
- [240. Search a 2D Matrix II (Medium)](https://leetcode.com/problems/search-a-2d-matrix-ii/) — the canonical staircase problem.
- [378. Kth Smallest Element in a Sorted Matrix (Medium)](https://leetcode.com/problems/kth-smallest-element-in-a-sorted-matrix/) — search on answer with staircase counter.
- [668. Kth Smallest Number in Multiplication Table (Hard)](https://leetcode.com/problems/kth-smallest-number-in-multiplication-table/) — same idea, virtual matrix.
- [4. Median of Two Sorted Arrays (Hard)](https://leetcode.com/problems/median-of-two-sorted-arrays/) — partition-based binary search.
- [719. Find K-th Smallest Pair Distance (Hard)](https://leetcode.com/problems/find-k-th-smallest-pair-distance/) — search on the gap.
- [1351. Count Negative Numbers in a Sorted Matrix (Easy)](https://leetcode.com/problems/count-negative-numbers-in-a-sorted-matrix/) — staircase counting.
- [1428. Leftmost Column With at Least a One (Medium)](https://leetcode.com/problems/leftmost-column-with-at-least-a-one/) — interactive matrix staircase.
- [162. Find Peak Element (Medium)](https://leetcode.com/problems/find-peak-element/) — 1D peak finder, prereq for 2D.
- [1901. Find a Peak Element II (Medium)](https://leetcode.com/problems/find-a-peak-element-ii/) — 2D binary search on the column with max.
