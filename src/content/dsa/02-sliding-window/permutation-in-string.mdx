---
title: "Permutation in String"
phase: "02-sliding-window"
order: 5
tags: ["string"]
premium: false
previewParagraphs: 4
estimatedMinutes: 6
prerequisites: []
summary: "Slide a fixed-size window across s2 and use a matches counter (not full map compare) to detect when the window's character frequencies equal s1's. O(n + σ)."
complexity:
  time: "O(n + σ)"
  space: "O(σ)"
commonPitfalls:
  - "Comparing full frequency maps each step is O(n·σ); the `matches` trick keeps it O(n)."
  - "Off-by-one initializing the first window — it's `s2[0..k-1]`."
  - "Updating `matches` with `>=` instead of exact equality — wrong; increment when count just hit the requirement, decrement when it just left."
  - "For 'find all anagrams' (LC 438), emit `l` instead of returning early."
  - "For unicode strings, swap the `[26]` array for a hash map."
relatedAnims: []
---

## Problem
Given strings `s1` and `s2`, return `true` if **some permutation of `s1`** appears as a contiguous substring of `s2`. Equivalently: does any window of length `|s1|` in `s2` have the same character frequencies as `s1`?

## Intuition
A permutation is fully characterized by its character-frequency map. Slide a fixed-size window of length `k = |s1|` across `s2`. Maintain a rolling frequency map for the window; compare against `need = Counter(s1)`. Naively comparing two maps every step is O(σ) per step — replace it with a **match counter** `matches` that increments when a character's window-count *equals* `need[ch]`, decrements when it stops matching. The window is a permutation iff `matches == 26` (over the lowercase-English alphabet). The invariant: each step does O(1) work, so the total is O(n + σ).

## Algorithm
1. If `|s1| > |s2|`, return `false`.
2. Build `need` and `have` as size-26 arrays. Initialize `matches`: number of letters where `have[i] == need[i]`.
3. For `r` in `k..n-1`:
   - Add `s2[r]` to `have`; update `matches`.
   - Remove `s2[r - k]` from `have`; update `matches`.
   - If `matches == 26`, return `true`.
4. Final check on the initial window, then return `false`.

```python
def check_inclusion(s1, s2):
    k, n = len(s1), len(s2)
    if k > n: return False
    need, have = [0]*26, [0]*26
    for ch in s1: need[ord(ch) - 97] += 1
    for i in range(k): have[ord(s2[i]) - 97] += 1
    matches = sum(1 for i in range(26) if need[i] == have[i])
    if matches == 26: return True
    for r in range(k, n):
        ai = ord(s2[r]) - 97
        have[ai] += 1
        if have[ai] == need[ai]: matches += 1
        elif have[ai] == need[ai] + 1: matches -= 1
        bi = ord(s2[r - k]) - 97
        have[bi] -= 1
        if have[bi] == need[bi]: matches += 1
        elif have[bi] == need[bi] - 1: matches -= 1
        if matches == 26: return True
    return False
```

## Walkthrough
On `s1 = "ab"`, `s2 = "eidbaooo"`, `k = 2`, `need = {a:1, b:1}`:

1. **Initial window** covers `s2[0..1] = "ei"`. `window = {e:1, i:1}` — nowhere near `need`.
2. Slide: `+'d', -'e'` → `"id"`, window = `{i:1, d:1}`. Still off.
3. Slide: `+'b', -'i'` → `"db"`, window = `{d:1, b:1}`. Closer — `b` matches.
4. Slide: `+'a', -'d'` → `"ba"`, window = `{b:1, a:1}` — **exactly equal to `need`**. Return `true`.

<Anim id="permutation-in-string" />

## Complexity

- time **O(n + σ)**
- space **O(σ)** alphabet size

## Pitfalls
- Comparing full frequency maps every step — works, but O(n·σ); use the `matches` trick to keep it O(n).
- Off-by-one when initializing the first window — it's `s2[0..k-1]`, not `s2[0..k]`.
- Updating `matches` on the wrong side — increment when count *just hit* the requirement; decrement when it *just left*. Use exact equality checks; `>=` is wrong.
- For "find all anagrams" (LC 438), the engine is identical — just emit `l` whenever `matches == 26` instead of returning.
- Unicode strings — replace the `[26]` array with a hash map.

## Practice — LeetCode

- [567. Permutation in String (Medium)](https://leetcode.com/problems/permutation-in-string/) — the canonical problem.
- [242. Valid Anagram (Easy)](https://leetcode.com/problems/valid-anagram/) — the frequency-map building block.
- [438. Find All Anagrams in a String (Medium)](https://leetcode.com/problems/find-all-anagrams-in-a-string/) — emit every matching window start.
- [49. Group Anagrams (Medium)](https://leetcode.com/problems/group-anagrams/) — key by sorted string or by frequency tuple.
- [1456. Maximum Number of Vowels in a Substring of Given Length (Medium)](https://leetcode.com/problems/maximum-number-of-vowels-in-a-substring-of-given-length/) — fixed window with rolling counter.
- [1100. Find K-Length Substrings With No Repeated Characters (Medium)](https://leetcode.com/problems/find-k-length-substrings-with-no-repeated-characters/) — fixed window, all-distinct check.
- [187. Repeated DNA Sequences (Medium)](https://leetcode.com/problems/repeated-dna-sequences/) — fixed window of size 10 plus a seen-set.
- [76. Minimum Window Substring (Hard)](https://leetcode.com/problems/minimum-window-substring/) — variable-size cousin.
- [30. Substring with Concatenation of All Words (Hard)](https://leetcode.com/problems/substring-with-concatenation-of-all-words/) — word-level permutation matching.
- [1396. Design Underground System (Medium)](https://leetcode.com/problems/design-underground-system/) — not a window per se, but the running-stat hash-map pattern shows up everywhere.
