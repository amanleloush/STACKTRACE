---
title: "DFS on Graph"
phase: "05-dfs"
order: 1
tags: ["graph"]
premium: false
previewParagraphs: 4
estimatedMinutes: 5
prerequisites: []
summary: "DFS commits to one neighbour and follows it down before trying the next. The recursion stack records the current path; visited prevents re-entry."
complexity:
  time: "O(V + E)"
  space: "O(V)"
commonPitfalls:
  - "Forgetting to mark the node visited before recursing — leads to infinite loops on cycles."
  - "Stack overflow on long chains (10^5+ depth) — convert to iterative with an explicit stack."
  - "On undirected graphs, treating a back-edge to the parent as a cycle — pass parent or use edge IDs."
  - "Mutating shared state in pre-order without undoing it in post-order when you need backtracking."
  - "Building graph as a list of edges and re-scanning it each call instead of an adjacency map."
relatedAnims: []
---

## Problem
Visit every node reachable from a starting vertex, exploring as deep as possible before backtracking.

## Intuition
DFS commits to one neighbour and follows it all the way down before trying the next. The **invariant** is that every node on the current recursion stack is on the path from the source to the deepest frontier — when the call returns, that node is fully explored. A `visited` set prevents re-entry, which turns the search graph into a DFS tree.

## Algorithm
Mark the start visited, recurse into each unvisited neighbour. The recursion stack itself records the current path; you can hook work into the *pre* position (before recursing) or the *post* position (after all children return) depending on what you want to compute.

```python
def dfs(node, graph, visited):
    visited.add(node)
    # pre-order work goes here
    for nei in graph[node]:
        if nei not in visited:
            dfs(nei, graph, visited)
    # post-order work goes here
```

## Walkthrough
1. Graph: `0 — 1 — 3` and `0 — 2`. Start at `0`.
2. Mark `0` visited. Pick neighbour `1`, recurse.
3. Mark `1` visited. Recurse into `3`. `3` has no new neighbours — return.
4. Back at `1` — no more neighbours, return to `0`.
5. From `0`, recurse into `2`. Mark `2`, return. Done.

<Anim id="dfs-graph" />

## Complexity

- time **O(V + E)**
- space **O(V)** recursion + visited

## Pitfalls
- Forgetting to mark the node visited **before** recursing — leads to infinite loops on cycles.
- Stack overflow on long chains (10⁵+ depth) — convert to iterative with an explicit stack.
- On undirected graphs, treating a back-edge to the parent as a cycle — pass parent or use edge IDs.
- Mutating shared state in pre-order without undoing it in post-order when you need backtracking.
- Building `graph` as a list of edges and re-scanning it each call instead of an adjacency map.

## Practice — LeetCode
- [200. Number of Islands (Medium)](https://leetcode.com/problems/number-of-islands/) — 2D DFS, the canonical flood fill.
- [547. Number of Provinces (Medium)](https://leetcode.com/problems/number-of-provinces/) — count connected components via DFS.
- [695. Max Area of Island (Medium)](https://leetcode.com/problems/max-area-of-island/) — return a value from each DFS call.
- [207. Course Schedule (Medium)](https://leetcode.com/problems/course-schedule/) — cycle detection in a directed graph.
- [210. Course Schedule II (Medium)](https://leetcode.com/problems/course-schedule-ii/) — topological order via post-order DFS.
- [130. Surrounded Regions (Medium)](https://leetcode.com/problems/surrounded-regions/) — DFS from borders inward.
- [417. Pacific Atlantic Water Flow (Medium)](https://leetcode.com/problems/pacific-atlantic-water-flow/) — two DFS passes from each ocean.
- [1376. Time Needed to Inform All Employees (Medium)](https://leetcode.com/problems/time-needed-to-inform-all-employees/) — DFS on a tree, take max.
- [841. Keys and Rooms (Medium)](https://leetcode.com/problems/keys-and-rooms/) — reachability from room 0.
- [1971. Find if Path Exists in Graph (Easy)](https://leetcode.com/problems/find-if-path-exists-in-graph/) — warm-up DFS reachability.
